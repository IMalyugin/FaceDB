.video { object-fit: cover; }

.videoContainer {
  display: flex;
  position: relative;
  overflow: hidden;
}

.canvas {
  display: none;
}

.overlay {
  position: absolute;
  top: 0;
  right: 0;
  left: 0;
  bottom: 0;
}
