@import "styles/constants.pcss";

.container {
  margin: 0;
  padding: 0;
  border: 1px solid var(--borderColor);
  border-collapse: collapse;

  td,
  th {
    border: 1px solid var(--borderColor);
    padding: 5px 15px;
  }
}
